import { styled } from '@mui/material';

export const FormWrapper = styled('div')({
  padding: '20px',
  background: 'white',
  position: 'absolute',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
});
